<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StandardOfSplit"
        tools:targetApi="31">
        <activity
            android:name=".View.Activity.ReceiptActivity"
            android:exported="false"
            android:label="@string/title_activity_receipt"
            android:theme="@style/Theme.StandardOfSplit" />
        <activity
            android:name=".View.Activity.CalculatorActivity"
            android:exported="false"
            android:label="@string/title_activity_calculator"
            android:theme="@style/Theme.StandardOfSplit" /> <!-- StartActivity가 앱의 첫 화면이 되도록 intent-filter를 추가 -->
        <activity
            android:name=".View.Activity.MainActivity"
            android:exported="true"
            android:label="@string/title_activity_start"
            android:theme="@style/Theme.StandardOfSplit">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>